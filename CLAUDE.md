@AGENTS.md

# Claude Code — Project Overrides

All project rules are in AGENTS.md, imported above so it actually loads into context at
session start. A markdown link alone does not load it; Claude would have to go and read the
file, and usually would not.

No Claude Code-specific overrides at this time.
